/* import { render, screen } from '@testing-library/react';
import App from '../components/App'; //componente que queremos testear

test('renders "Hola mundo"', () => { //declaramos el test
    // Arrange (Función donde decimos que nos renderice componente App)
    render(<App />);
    // Act (Buscamos dentro de lo renderizado, la etiqueta html que contiene el 'hola mundo')
    const divElement = screen.getByText('Hola mundo');
    // Assert (Indicamos que esperamos que divElement esté presente en el documento)
    expect(divElement).toBeInTheDocument();
}); */